apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}-node-service
spec:
  type: ClusterIP
  selector:
    podName: {{ .Values.appName }}-pod
  ports:
  - name: {{ .Values.appName }}-service
    port: 8443
    targetPort: 8443